let resposta = parseInt(prompt("Digite seu número de usuario"))

while (resposta != 0) {

    resposta = parseInt(prompt("Digite seu número de usuario"))

    if (resposta == 0) {
        alert("Numero de usúario correto");
    }
}
